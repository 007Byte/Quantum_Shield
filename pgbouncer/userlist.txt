; PLACEHOLDER — not used at runtime. The container's /etc/pgbouncer/userlist.txt
; is generated by entrypoint.sh on start (md5 for dev, the supplied SCRAM
; verifier for prod). This committed file is documentation only and is NOT
; mounted into the container. See pgbouncer/entrypoint.sh.
"usbvault" "md5password_placeholder"
