services:
  postgres:
    image: postgres:16-alpine
    container_name: usbvault-postgres
    environment:
      POSTGRES_DB: usbvault_dev
      POSTGRES_USER: usbvault
      POSTGRES_PASSWORD: dev_password_change_me
      PGTZ: UTC
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./migrations:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U usbvault"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: usbvault-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    container_name: usbvault-minio
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 5

  createbuckets:
    image: minio/mc:latest
    container_name: usbvault-createbuckets
    depends_on:
      minio:
        condition: service_healthy
    entrypoint: >
      /bin/sh -c "
      mc alias set myminio http://minio:9000 minioadmin minioadmin;
      mc mb --ignore-existing myminio/usbvault-encrypted-blobs;
      mc version enable myminio/usbvault-encrypted-blobs;
      exit 0;
      "

  api:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    container_name: usbvault-api
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      minio:
        condition: service_healthy
      createbuckets:
        condition: service_completed_successfully
    environment:
      # sslmode=disable is acceptable for local dev via Docker internal network
      # For production, use sslmode=require or sslmode=verify-full
      DATABASE_URL: postgres://usbvault:dev_password_change_me@postgres:5432/usbvault_dev?sslmode=disable
      REDIS_URL: redis://redis:6379
      S3_ENDPOINT: http://minio:9000
      S3_BUCKET: usbvault-encrypted-blobs
      S3_ACCESS_KEY: minioadmin
      S3_SECRET_KEY: minioadmin
      AWS_ACCESS_KEY_ID: minioadmin
      AWS_SECRET_ACCESS_KEY: minioadmin
      AWS_REGION: us-east-1
      SERVER_PORT: 8080
      LOG_LEVEL: debug
      ENVIRONMENT: development
      FIDO2_RELYING_PARTY_ID: localhost
      FIDO2_RELYING_PARTY_NAME: USBVault Dev
      FIDO2_RELYING_PARTY_ORIGIN: http://localhost:3000
      JWT_ED25519_PRIVATE_KEY: ""
      JWT_ED25519_PUBLIC_KEY: ""
      MIGRATIONS_DIR: /app/migrations
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
  redis_data:
  minio_data:

networks:
  default:
    name: usbvault-network
